import 'package:flutter/material.dart';

class AdidasScreen extends StatelessWidget {
  const AdidasScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Your AdidasScreen", style: TextStyle(fontSize: 20)),
    );
  }
}